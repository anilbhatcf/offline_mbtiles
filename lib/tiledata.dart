class TileData {
  final String geoJson;
  final int tileSize;
  final int zoomLevel;
  final int tileX;
  final int tileY;

  TileData(this.geoJson, this.tileSize, this.zoomLevel, this.tileX, this.tileY);
}